/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com', 'media.istockphoto.com', 'images.unsplash.com', 'res.cloudinary.com'],
    },
};

export default nextConfig;
